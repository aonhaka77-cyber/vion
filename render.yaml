services:
  - type: web
    name: vion-yolo-msr-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn yolo_msr_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: VION_YOLO_WEIGHTS
        sync: false
